#include <stdio.h>

unsigned int	ft_djb2(unsigned char *)
{
	unsigned long hash;
	int	c;

	hash = 5381;
	while (c = *str++)
		hash = ((hash << 5) + hash) + c;
	return (hash);
}

int	main(void)
{
	char	***word;
	int	len_word;
	int	len_case_test;
	int	current_case_i;
	int	current_word_i;
	int	current_player_i;

	scanf("%d\n", &len_word);
	current_case_i = 0;
	word = (char ***)malloc(sizeof(char **) * 3);
	while (i < len_word)
	{
		current_word_i = 0;z
		scanf("%d");
		i++;
	}
	free(word);
	return (0);
}
